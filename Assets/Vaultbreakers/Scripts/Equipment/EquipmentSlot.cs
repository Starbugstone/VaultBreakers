namespace Vaultbreakers.Equipment
{
    /// <summary>
    /// Stable visual equipment slots. Values are serialized, so append new slots instead of reordering them.
    /// </summary>
    public enum EquipmentSlot
    {
        Helmet = 0,
        Armor = 1,
        Melee = 2,
        Ranged = 3,
        Shield = 4,
        Rig = 5
    }
}
